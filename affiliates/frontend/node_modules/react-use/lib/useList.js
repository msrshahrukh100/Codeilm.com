"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = require("react");
var useList = function (initialList) {
    if (initialList === void 0) { initialList = []; }
    var _a = react_1.useState(initialList), list = _a[0], set = _a[1];
    return [list, {
            set: set,
            updateAt: function (index, entry) { return set(list.slice(0, index).concat([
                entry
            ], list.slice(index + 1))); },
            remove: function (index) { return set(list.slice(0, index).concat(list.slice(index + 1))); },
            push: function (entry) { return set(list.concat([entry])); },
            filter: function (fn) { return set(list.filter(fn)); },
            sort: function (fn) { return set(list.slice().sort(fn)); },
        }];
};
exports.default = useList;
